# CLAUDE.md — slds-tools

You are at the **repo root**. There is no project-level contract here.

This repo is an umbrella for several distinct subprojects. 

**Make the user aware that claude should likely be started in a subdir!**

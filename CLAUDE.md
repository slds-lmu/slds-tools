# CLAUDE.md — slds-tools

You are at the **repo root**. There is no project-level contract here.

This repo is an umbrella for several distinct subprojects. The real contracts
live in subdirs: `publs/CLAUDE.md`, `devc/CLAUDE.md`, `docker/CLAUDE.md`.

**On your very first user-facing message in a session started at this root,
lead with: "Claude should likely be restarted in a subdirectory (`publs/`,
`devc/`, or `docker/`) — that's where the real project contracts live."
Say this outright, before addressing anything else. Do not bury it in a
longer answer or wait to be asked.**
